// import { useStoreState } from "easy-peasy"

// const useOrders = (id) => {
//     const {data, loading, error} = useStoreState((state) => state.order)

//     const findOrderForSingleUser = data && data.filter((item) => Number(item.userId) === Number(id))

//     return {
//         findOrderForSingleUser, loading, error
//     }
// }

// export default useOrders